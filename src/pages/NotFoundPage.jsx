const NotFoundPage = () => {
  // function that will direct the user to "page not found" if the page doesn't exist or theres another problem(with axios, etc).
  return <h1>Page not found </h1>;
};

export default NotFoundPage;
